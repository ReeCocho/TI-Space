#ifndef SPC_PALETTE_H
#define SPC_PALETTE_H

/* Includes. */
#include <graphx.h>
#include <stdint.h>

/* Number of elements in color palette. */
#define SPC_COLOR_COUNT 6

/* Color palette data. */
extern uint16_t spc_color_palette[SPC_COLOR_COUNT];

#endif